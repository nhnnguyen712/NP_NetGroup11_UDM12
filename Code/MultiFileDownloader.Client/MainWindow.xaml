<Window x:Class="MultiFileDownloader.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Multi File Downloader"
        Height="450"
        Width="900">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- SERVER FILE LIST -->

        <ListBox x:Name="lbServerFiles"
         Grid.Row="0"
         Grid.Column="0"
         SelectionMode="Extended"
         PreviewMouseMove="lbServerFiles_PreviewMouseMove"/>

        <!-- DOWNLOAD LIST -->

        <ListView x:Name="lvDownloads"
          Grid.Row="0"
          Grid.Column="1"
          AllowDrop="True"
          Drop="lvDownloads_Drop">

            <ListView.View>

                <GridView>

                    <GridViewColumn Header="File Name"
                DisplayMemberBinding="{Binding FileName}"
                Width="220"/>

                    <GridViewColumn Header="Progress" Width="220">

                        <GridViewColumn.CellTemplate>

                            <DataTemplate>

                                <ProgressBar Value="{Binding Progress}"
             Minimum="0"
             Maximum="100"
             Width="180"
             Height="16"/>

                            </DataTemplate>

                        </GridViewColumn.CellTemplate>

                    </GridViewColumn>

                    <GridViewColumn Header="Speed"
                DisplayMemberBinding="{Binding Speed}"
                Width="120"/>

                </GridView>

            </ListView.View>

        </ListView>

        <!-- BUTTON PANEL -->

        <StackPanel Grid.Row="1"
            Grid.ColumnSpan="2"
            Orientation="Horizontal"
            HorizontalAlignment="Center">

            <Button Content="Download Selected"
        Width="160"
        Margin="10"
        Click="Download_Click"/>

            <Button Content="Open Downloads Folder"
        Width="200"
        Margin="10"
        Click="OpenDownloads_Click"/>

        </StackPanel>

    </Grid>

</Window>